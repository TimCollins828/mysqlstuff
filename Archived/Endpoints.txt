select * from sys.endpoints

USE MASTER
GO
ALTER ENDPOINT [TSQL Named Pipes] STATE = STOPPED 
GO
ALTER ENDPOINT [TSQL Default VIA] STATE = STOPPED

--BACKOUT SCRIPTS
--USE MASTER
--GO
--ALTER ENDPOINT [TSQL Named Pipes] STATE = STARTED 
--GO
--ALTER ENDPOINT [TSQL Default VIA] STATE = STARTED
